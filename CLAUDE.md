# SOP 文件站

這是 MkDocs Material 架的公司內部 SOP 文件站（部署到 GitHub Pages）。使用者是非技術背景的營運人員，用口述／截圖／PDF 提供內容，由 AI 負責實際寫檔案、排版、維護網站架構。

## 開始改動前，一定要先讀 [ARCHITECTURE.md](ARCHITECTURE.md)

裡面完整記錄了這個站的所有慣例與陷阱：連結集中管理（`{{ links.xxx }}`）、自動連結彙整、自動目錄、常用連結收藏系統、表格智慧換行的陷阱、分類主題色怎麼加、`overrides/` 不被 `mkdocs serve` 監看要怎麼強制重建、5 個獨立 HTML 檔案的特殊限制等等。**這份文件必須跟著每次改動同步更新**，改了架構、加了新的自動化功能、拿掉舊功能，都要回來補上對應段落，不要讓它跟實際狀況脫節。

## 這個專案的工作方式

- 使用者不寫程式碼，所有實作（檔案編輯、排版、連結登記、build 驗證）都由 AI 完成
- 內容多半是使用者一段一段口述或貼截圖給的，不要假設細節、缺的資訊直接問
- 任何網址如果會用在兩個以上地方，一律先登記進 `mkdocs.yml` 的 `extra.links`，不要貼裸網址
- 改完 `.md` 內容或 `overrides/main.html` 後，要用 `python -m mkdocs build` 驗證沒有錯誤，並觸發本地 `mkdocs serve` 重新整理（碰一下 `docs/index.md` 的修改時間）給使用者預覽
- 只有使用者明確說「push」才 commit + push，不要自己主動推
